# 詳細バックアップ

## 作成日時
$(date '+%Y-%m-%d %H:%M:%S %Z')

## Git情報
### 現在のブランチ
$(git branch --show-current)

### 最新のコミット
$(git log --oneline -1)

### 直近50件のコミット履歴
$(git log --oneline -50)

## 重要なファイルの状態

### netlify.toml
$(cat netlify.toml)

### 未コミットの変更
$(git status)

## 重要な変更のサマリー

### バックエンド (meta_api.py)
- 広告セット/広告レベルのデータ取得処理を追加
- 全期間データ取得のデバッグログ追加
- 日付範囲フィルタリング改善

### フロントエンド (Dashboard.tsx)
- 日付範囲フィルタリングのデバッグログ追加
- propData優先表示ロジック
- Netlifyデプロイ用の変更

### Netlify設定
- base = "frontend" 設定
- publish = "dist" 設定
